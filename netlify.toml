[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Proxy all /api calls to the Railway backend
[[redirects]]
  from = "/api/*"
  to = "https://rfi-engine-production.up.railway.app/:splat"
  status = 200
  force = true

# SPA fallback — serve index.html for all frontend routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
