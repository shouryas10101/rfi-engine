# In dev, leave this blank — Vite proxies /api to the backend on :4000.
# In production (Netlify), set this to your backend URL, e.g.
# VITE_API_BASE_URL=https://rfi-engine-backend.onrender.com
VITE_API_BASE_URL=
